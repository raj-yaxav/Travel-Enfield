MONGODB_URI=mongodb+srv://USERNAME:PASSWORD@CLUSTER.mongodb.net/travelenfield?retryWrites=true&w=majority
PORT=4000

# SMTP credentials used to email login OTP codes (see server/mailer.js).
# Example for Gmail: create an "App Password" at https://myaccount.google.com/apppasswords
# (regular Gmail passwords will not work), then set SMTP_HOST=smtp.gmail.com, SMTP_PORT=465, SMTP_SECURE=true.
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false


SMTP_FROM="TravelEnfield <no-reply@travelenfield.in>"



# Admin panel login (/admin). Generate a hash with:
# node -e "require('bcryptjs').hash('yourpassword',10).then(console.log)"
# IMPORTANT: escape every $ as \$ in the hash below, or Next.js's env expansion will corrupt it.
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD_HASH=\$2b\$10\$replace-with-a-generated-bcrypt-hash
ADMIN_SESSION_SECRET=replace-with-a-random-64-char-hex-string
